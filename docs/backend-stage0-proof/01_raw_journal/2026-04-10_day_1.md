# Build Journal - Day 1: 2026-04-10

## SESSION START: 2026-04-10T12:07:20+01:00

- current day/date: Day 1 / 2026-04-10
- target gate: Gate A
- checkpoint: repo scan in the current working directory is complete; the repo is a kit-only overlay and not a deployable app
- blockers:
  - no application code, server entrypoint, or package/runtime manifest exists in this repo
  - route ownership for `GET /api/classify` is therefore not confirmable
  - deployment path and access cannot be confirmed without the actual app surface
- next three actions:
  - request the real target repo or approval to scaffold a minimal app in this repo
  - confirm the accepted deployment platform and access path for that app
  - start the local contract shell once route ownership is explicit
- logs to update this session:
  - `docs/backend-stage0-sprint/07_progress_tracker.md`
  - `docs/backend-stage0-sprint/08_evidence_log.md`
  - `docs/backend-stage0-sprint/09_decisions_log.md`
  - `docs/backend-stage0-delivery/06_progress_tracker.md`
  - `docs/backend-stage0-delivery/07_evidence_log.md`
  - `docs/backend-stage0-delivery/08_decisions_log.md`
  - `docs/backend-stage0-proof/06_progress_tracker.md`
  - `docs/backend-stage0-proof/07_evidence_log.md`
  - `docs/backend-stage0-proof/08_decisions_log.md`
  - `docs/agent-ops/06_delivery-state.md`

## Session focus

- complete the Day 1 repo scan in the current working directory
- verify the required artifacts are present
- resolve route ownership and stand up the first working local implementation

## What I changed

- read the required sprint prompts, agent-ops files, and current trackers
- scanned the repo tree and confirmed that the current working directory initially contained only the Stage 0 kit files
- recorded the initial blocker state, then resumed after human confirmation that this repo should become the implementation repo
- added `package.json` and `.gitignore` for a no-dependency Node.js 22 setup
- implemented the shared request logic in `src/classify-service.js`
- added `local-server.js` for local development and `server.js` for the production Vercel entrypoint
- added `test/classify.test.js` covering success, validation, no-prediction, `502`, `500`, and preflight handling
- replaced the kit README with implementation-focused local-run and contract documentation
- installed and authenticated the Vercel CLI, then deployed the endpoint publicly
- updated the sprint, delivery, and proof trackers to reflect the real code and verification state

## Decision points

- kept the default no-prediction `422` decision unchanged
- after human confirmation, chose a minimal Node.js 22 stack with no runtime dependencies
- chose `server.js` as the Vercel deployment route owner and `local-server.js` as the local parity entrypoint
- mapped malformed upstream payloads to `502` instead of `500`

## What broke and how I fixed it

- `git status --short` failed because the current working directory is not a git repo
- the repo scan found no `package.json`, `pyproject.toml`, `go.mod`, `*.csproj`, `Dockerfile`, `Procfile`, `vercel.json`, or `railway.json`
- `npm test` initially failed when `node --test` attempted to spawn subprocesses in the sandbox; I changed the test script to `node test/classify.test.js`
- direct sandbox access to `api.genderize.io` failed with `connect EACCES`; I captured that honestly and then ran one approved escalated local success smoke to verify the real integration path
- the first Vercel production deploy returned `FUNCTION_INVOCATION_FAILED`; `vercel logs` showed `Invalid export found in module "/var/task/server.cjs"`
- deleting `server.js` to force an `api/` route path caused the next deploy to fail with `No entrypoint found`; I restored `server.js` as the production entrypoint function and kept `local-server.js` as the local-only process host

## Verification captured

- confirmed the required sprint, delivery, and proof artifact files already exist in the expected paths
- confirmed the route now exists locally at `/api/classify`
- `npm test` passed with 11 contract and edge-case tests
- local HTTP smoke confirmed `400` for missing `name`, `422` for repeated `name`, and `Access-Control-Allow-Origin: *`
- approved escalated local HTTP smoke confirmed a real `200` success response from Genderize for `name=john`
- approved escalated local burst smoke confirmed eight consecutive local `200` responses for `name=john`
- approved escalated live smoke confirmed `200` success for `john`
- approved escalated live smoke confirmed `200` success for `mary`
- approved escalated live smoke confirmed `400` for missing `name`
- approved escalated live smoke confirmed `204` preflight handling and `Access-Control-Allow-Origin: *`
- approved escalated live smoke confirmed five consecutive public `200` responses for repeated requests

## Next starting point

- confirm the GitHub repo URL that will be submitted
- confirm the full name and email for submission
- run the final submission step once those fields are available

## SESSION END: 2026-04-10T12:55:41+01:00

- what changed:
  - installed and authenticated the Vercel CLI
  - deployed the endpoint publicly to `https://backend-wizards-stage0.vercel.app`
  - recovered a failed first deploy by switching the Vercel production surface to `server.js`
  - updated README, sign-off, submission checklist, and all sprint/delivery/proof trackers with live state
- checks run:
  - `npm test`
  - repeated local smoke against `http://127.0.0.1:3000/api/classify?name=john`
  - `vercel deploy --prod --yes --force --format json`
  - `vercel logs --since 15m --status-code 500 --no-follow --expand`
  - live smoke against `https://backend-wizards-stage0.vercel.app/api/classify`
- files updated:
  - `server.js`
  - `local-server.js`
  - `README.md`
  - sprint, delivery, proof, and agent-ops artifacts
- blockers remaining:
  - final submission has not been executed yet, so the timestamp and bot outcome are still unknown
- next exact starting point:
  - perform the final submission with the confirmed packet fields
  - record the submission timestamp
  - record the bot outcome
- next handoff point:
  - Gate D final submission readiness with all submission fields complete
